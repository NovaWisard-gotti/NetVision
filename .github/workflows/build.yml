name: Build NetVision APK

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch: {}

jobs:
  build:
    name: Build debug and release APK
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: true

      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: gradle-${{ runner.os }}-${{ hashFiles('android/gradle/wrapper/gradle-wrapper.properties', 'android/settings.gradle', 'android/build.gradle', 'android/app/build.gradle') }}
          restore-keys: |
            gradle-${{ runner.os }}-

      - name: Flutter doctor
        run: flutter doctor -v

      - name: Install dependencies
        run: flutter pub get

      - name: Generate launcher icons
        run: dart run flutter_launcher_icons

      - name: Static analysis
        shell: bash
        run: |
          set +e
          flutter analyze > analyze_output.txt 2>&1
          status=$?
          cat analyze_output.txt
          if [ $status -ne 0 ]; then
            echo "::error::flutter analyze exited with status $status"
            awk 'NF' analyze_output.txt | tail -n 60 | while IFS= read -r line; do
              echo "::error::${line}"
            done
          fi
          exit $status

      - name: Run unit tests
        shell: bash
        run: |
          set +e
          flutter test > test_output.txt 2>&1
          status=$?
          cat test_output.txt
          if [ $status -ne 0 ]; then
            echo "::error::flutter test exited with status $status"
            awk 'NF' test_output.txt | tail -n 80 | while IFS= read -r line; do
              echo "::error::${line}"
            done
          fi
          exit $status

      - name: Build debug APK
        shell: bash
        run: |
          set +e
          flutter build apk --debug > build_debug_output.txt 2>&1
          status=$?
          cat build_debug_output.txt
          if [ $status -ne 0 ]; then
            echo "::error::flutter build apk --debug exited with status $status"
            awk 'NF' build_debug_output.txt | tail -n 80 | while IFS= read -r line; do
              echo "::error::${line}"
            done
          fi
          exit $status

      - name: Build release APK
        shell: bash
        run: |
          set +e
          flutter build apk --release > build_release_output.txt 2>&1
          status=$?
          cat build_release_output.txt
          if [ $status -ne 0 ]; then
            echo "::error::flutter build apk --release exited with status $status"
            awk 'NF' build_release_output.txt | tail -n 80 | while IFS= read -r line; do
              echo "::error::${line}"
            done
          fi
          exit $status

      - name: Upload debug APK
        uses: actions/upload-artifact@v4
        with:
          name: netvision-debug-apk
          path: build/app/outputs/flutter-apk/app-debug.apk

      - name: Upload release APK
        uses: actions/upload-artifact@v4
        with:
          name: netvision-release-apk
          path: build/app/outputs/flutter-apk/app-release.apk
